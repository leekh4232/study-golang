# 변수와 데이터 타입

## #01. 변수 사용 방법

### 1. `var` 키워드를 사용한 변수 선언

변수의 타입을 명시적으로 지정

```go
var 변수이름 타입
```

#### 예시

```go
var x int // 정수형 변수 x 선언
var name string // 문자열 변수 name 선언
```

### 2. 값의 할당

대입연산자 `=`을 사용하여 값을 할당

```go
var 변수이름 타입
변수이름 = 값
```

#### 예시

```go
var x int
x = 10
```

### 3. 선언과 할당의 통합

```go
var 변수이름 타입 = 값
```

#### 예시

```go
var x int = 10
```

### 4. `:=` 단축 선언 연산자 사용

변수 선언 시 `:=` 를 사용하여 타입을 생략하고 값을 자동으로 추론함

이 방법은 함수 내부에서만 사용 가능

> 권장하지는 않음

```go
변수이름 := 값
```

#### 예시

```go
x := 10 // x는 int 타입으로 자동 추론
```

### 5. 여러 변수 선언

여러 변수를 한 번에 선언할 수도 있다.

`var` 키워드를 사용하여 여러 변수를 선언할 때는 각 변수의 타입을 명시하거나 값으로 초기화할 수 있다.

#### 타입을 명시한 경우

```go
var 변수이름1, 변수이름2 타입
```

##### 예시

```go
var x, y int
```

#### 값을 초기화한 경우

```go
var 변수이름1, 변수이름2 타입 = 값1, 값2
```

##### 예시

```go
var x, y int = 100, 200
```

#### 단축 선언 연산자

```go
변수이름1, 변수이름2 := 값1, 값2
```


##### 예시

```go
x, y := 10, 20
```

## #02. 상수 선언

`const` 키워드를 사용

```go
const Pi = 3.14
```

## #03. 전역 변수 선언

전역 변수는 var 키워드를 사용하여 함수 외부에서 선언.

이 경우 모든 함수에서 접근 가능

## #04. 데이터 타입

| 분류        | 데이터 타입   | 설명                                    | 예시 값                 | 메모리 크기    |
|-------------|---------------|-----------------------------------------|-------------------------|----------------|
| **기본형**     | **`int`**         | 정수형 (크기는 플랫폼에 따라 다름)      | `42`, `-10`             | 4 또는 8 바이트 (플랫폼에 따라 다름) |
| 기본형     | `int8`        | 8비트 정수형                           | `-128`, `127`           | 1 바이트        |
| 기본형     | `int16`       | 16비트 정수형                          | `-32768`, `32767`       | 2 바이트        |
| 기본형     | `int32`       | 32비트 정수형                          | `-2147483648`, `2147483647` | 4 바이트        |
| 기본형     | `int64`       | 64비트 정수형                          | `-9223372036854775808`, `9223372036854775807` | 8 바이트        |
| **기본형**     | **`uint`**        | 부호 없는 정수형 (크기는 플랫폼에 따라 다름) | `0`, `42`               | 4 또는 8 바이트 (플랫폼에 따라 다름) |
| 기본형     | `uint8`       | 8비트 부호 없는 정수형                 | `0`, `255`              | 1 바이트        |
| 기본형     | `uint16`      | 16비트 부호 없는 정수형                | `0`, `65535`            | 2 바이트        |
| 기본형     | `uint32`      | 32비트 부호 없는 정수형                | `0`, `4294967295`       | 4 바이트        |
| 기본형     | `uint64`      | 64비트 부호 없는 정수형                | `0`, `18446744073709551615` | 8 바이트        |
| **기본형**     | **`float32`**     | 32비트 부동소수점 숫자                 | `3.14`, `-2.5`          | 4 바이트        |
| **기본형**     | **`float64`**     | 64비트 부동소수점 숫자                 | `3.14159265359`, `-0.1234` | 8 바이트        |
| 기본형     | `complex64`   | 32비트 실수와 32비트 허수로 이루어진 복소수 | `1 + 2i`, `-3 - 4i`     | 8 바이트        |
| 기본형     | `complex128`  | 64비트 실수와 64비트 허수로 이루어진 복소수 | `1 + 2i`, `-5.6 - 3.2i` | 16 바이트       |
| **기본형**     | **`bool`**        | 불리언 값 (참 또는 거짓)                | `true`, `false`         | 1 바이트        |
| **기본형**     | **`string`**      | 문자열 (UTF-8 인코딩)                  | `"Hello, Go!"`, `"123"` | 문자열 길이에 따라 다름 (각 문자 1바이트 이상) |
| 기본형     | `byte`        | 8비트 부호 없는 정수형 (일반적으로 문자 표현) | `'a'`, `'Z'`            | 1 바이트        |
| 기본형     | `rune`        | 32비트 유니코드 문자                   | `'a'`, `'한'`           | 4 바이트        |
| 기본형     | `uintptr`     | 포인터를 표현할 수 있는 부호 없는 정수형  | `0`, `12345678`         | 4 또는 8 바이트 (플랫폼에 따라 다름) |
| **연속형**     | **`interface{}`** | 빈 인터페이스 (모든 타입을 받을 수 있음) | `42`, `"Hello"`, `true` | 16 바이트 (내부 구현에 따라 다를 수 있음) |
| 연속형     | `array`       | 고정 크기의 배열                        | `[1, 2, 3]`             | 크기 x 요소 크기 (예: `int` 배열의 경우 `n * 4` 바이트) |
| 연속형     | `slice`       | 가변 크기의 배열                        | `[]int{1, 2, 3}`        | 24 바이트 + `n` (슬라이스 길이에 따라 다름) |
| 연속형     | `map`         | 키-값 쌍으로 구성된 집합                | `map[string]int{"a": 1, "b": 2}` | 크기와 내용에 따라 다름 |
| 연속형     | `struct`      | 필드들을 묶은 구조체                    | `struct { x int; y int }` | 각 필드의 크기 합 |
| 연속형     | `chan`        | 채널                                   | `make(chan int)`        | 8 바이트 (플랫폼에 따라 다를 수 있음) |

### 설명

- **기본형 (Primitive Types)**: Go에서 제공하는 기본 데이터 타입으로, 숫자, 문자열, 논리형, 복소수 등이 포함.
- **연속형 (Composite Types)**: 여러 값들을 묶어서 다룰 수 있는 데이터 타입으로, 배열, 슬라이스, 맵, 구조체, 채널 등이 포함.

### 메모리 크기

**int**와 **uint**는 플랫폼에 따라 다르므로 32비트 플랫폼에서는 4바이트, 64비트 플랫폼에서는 8바이트로 할당됨.

슬라이스의 크기는 기본적으로 슬라이스 자체가 24바이트로 시작하고, 그 이후 요소들이 추가됨에 따라 크기가 달라짐.

배열은 고정된 크기를 가지므로 크기는 `요소 수 * 요소 크기`로 계산됩니다. 예를 들어 `int` 배열은 각 요소가 `4바이트`이므로, `n`개의 요소를 가진 배열은 `n * 4 바이트`가 됨
